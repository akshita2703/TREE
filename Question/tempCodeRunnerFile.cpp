 // int leftChildData, rightChildData;
        // cin >> leftChildData >> rightChildData; // Left and right child nodes' data

        // if (leftChildData != -1) {
        //     parent->left = new TreeNode(leftChildData);
        //     q.push(parent->left);
        // }

        // if (rightChildData != -1) {
        //     parent->right = new TreeNode(rightChildData);
        //     q.push(parent->right);
        // }